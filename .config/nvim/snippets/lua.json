{
    "plugin init pcall": {
        "prefix": "plinit",
        "body": [
            "local status_ok, $1 = pcall(require, \"$2\")",
            "if not status_ok then",
            "    return",
            "end",
            "$0"
        ],
        "description": "Nvim plugin setup pcall"
    }
}
